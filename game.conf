title = Lumo - Oficina dos Mundos
description = Um mundo de construção, descoberta e criatividade.

# lumo_core é o event bus do jogo e precisa existir antes de qualquer outro mod.
# Restrição da engine (src/content/mod_configuration.cpp:252): um mod declarado
# como first_mod NÃO pode ter `depends` nem `optional_depends` no seu mod.conf.
first_mod = lumo_core
