# Mundos de teste gerados pelo smoke_test.sh ficam fora do jogo, mas se alguém
# rodar a engine com o jogo em outro layout, isto evita commitar mundo.
worlds/

# Binários auxiliares de validação (luac/lua compilados do lib/lua da engine).
.tools/

# Lixo de editor e de sistema.
*.swp
*~
.DS_Store
Thumbs.db
