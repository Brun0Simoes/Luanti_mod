name = lumo_core
description = Núcleo do Lumo: event bus e comunicação entre sistemas.

# NÃO adicione `depends` nem `optional_depends` aqui.
# Este mod é o `first_mod` do jogo (game.conf) e a engine rejeita o jogo
# inteiro se um first_mod declarar dependências
# (src/content/mod_configuration.cpp:252).
